# Jarvis_AI_Assistent
is a conversational AI agent built in Python, running a large language model (LLM) locally via Ollama. The agent understands user queries, automatically detects the language being used, and responds in that same language. 
